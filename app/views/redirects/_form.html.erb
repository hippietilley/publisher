<%= form_for @redirect do |f| %>
  <%= render "admin/form_errors", object: @redirect %>

  <div class="form-group">
    <%= f.label :source_path %>
    <%= f.text_field :source_path, class: "form-control input-lg" %>
  </div>
  
  <div class="form-group">
    <%= f.label :target_path %>
    <%= f.text_field :target_path, class: "form-control input-lg" %>
  </div>
  
  <%= render "admin/form_actions", f: f, cancel_url: redirects_url %>
<% end %>
