<h1>Editing Bookmark</h1>

<%= form_for @post, url: @post.path do |f| %>
  <%= render "form", f: f %>
<% end %>

<%= link_to "Delete", @post.path, method: :delete, data: { confirm: "Are you sure you're sure? There is NO UNDO." } if signed_in? %>
