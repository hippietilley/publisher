<ol class="h-feed">
  <%= render partial: "posts/post", collection: @posts %>
</ol>

<%= will_paginate @posts,
                  previous_label: "&larr;",
                  next_label:     "&rarr;",
                  inner_window:   2,
                  outer_window:   2 %>
