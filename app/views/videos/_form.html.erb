<%= render "admin/form_errors", thing: post %>

<%= render "admin/form_title_subtitle", f: f %>
<%= render "admin/form_content", f: f %>

<div class="form-group">
  <%= f.label :enclosure_url, "Video URL" %>
  <%= f.url_field :enclosure_url, class: "form-control" %>
</div>

<%= render "admin/form_actions",   f: f, cancel_url: videos_url %>

<fieldset>
  <legend class="h5">Video Meta</legend>
  <%= render "admin/form_image_url", f: f %>
  <%= render "admin/datetime_group", f: f, attr: :captured_at, thing: post %>
</fieldset>

<%= render "admin/form_actions",   f: f, cancel_url: videos_url %>
<%= render "admin/form_post_meta", f: f, post: post %>
<%= render "admin/form_actions",   f: f, cancel_url: videos_url %>


