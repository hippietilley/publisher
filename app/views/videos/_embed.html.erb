<video controls width="<%= post.width %>" height="<%= post.height %>" poster="<%= post.image_url %>" class="u-featured">
  <source src="<%= post.enclosure_url %>" class="u-video" />
  <img src=<%= post.image_url %>" class="u-photo" />
</video>
